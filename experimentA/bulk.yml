---
continue: "true"
transfer: "ln_s"
exclude: "clientpath"
skip: "all"
logprefix: "logs/"
output: "yaml"
parallel_upload: "10"
parallel_fileset: "10"
# Default columns for the regular screens.
# This may need to be modified in other bulk files.
columns:
    - target
    - path
